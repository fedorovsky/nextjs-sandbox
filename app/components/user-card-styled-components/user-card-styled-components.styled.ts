import { styled } from 'styled-components';
import { theme } from '@plarium/d2c-ui-theme';

export const Title = styled.h1`
  font-size: 24px;
  font-weight: bold;
  color: ${theme.system['200']};
`;
